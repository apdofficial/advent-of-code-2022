#pragma once

#include <istream>
#include <iomanip>
#include <vector>
#include <iostream>
#include <fstream>
#include <span>
#include <optional>

namespace aoc::day6 {

    auto start_of_pocket_marker(std::istream& packet) -> std::size_t;
}