#pragma once

#include <istream>
#include <iomanip>
#include <vector>
#include <iostream>
#include <fstream>
#include <span>

namespace aoc::day6 {

    auto start_of_pocket_marker(const std::istringstream& packet) -> int;
}